<Window x:Class="GraphViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:GraphViewerGdi="clr-namespace:Microsoft.Msagl.GraphViewerGdi;assembly=Microsoft.Msagl.GraphViewerGdi"
        mc:Ignorable="d"
        Title="Dependencies Viewer" 
        Loaded="MainWindow_OnLoaded"
        xmlns:ViewModel="clr-namespace:GraphViewer"
        d:DataContext="{d:DesignInstance ViewModel:MainWindowViewModel}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <Label Margin="5" Target="{Binding ElementName=AssemblyNameTextBox}">Assembly:</Label>
            <TextBox x:Name="AssemblyNameTextBox" Margin="7" Width="200"/>
            <Button Margin="5" Width="100" Command="{Binding SelectAssemblyCommand}">Select</Button>
        </StackPanel>
        
        <Button Grid.Row="1" Width="200" Margin="5" HorizontalAlignment="Left">Load Assembly</Button>

        <WindowsFormsHost Grid.Row="2" x:Name="WindowsFormsHost">
            <GraphViewerGdi:GViewer x:Name="graphViewer"/>
        </WindowsFormsHost>

    </Grid>
</Window>
